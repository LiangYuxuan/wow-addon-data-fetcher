import {buildTradeSkill} from './../modules/SavedInstances/TradeSkill';

(async () => {
    // const result = await buildTradeSkill(10); // Legion
    const result = await buildTradeSkill(13); // Dragonflight

    console.log(result);
})();
